{
  "name": "typescript-data-structures",
  "version": "1.0.0",
  "description": "Data structures in TypeScript",
  "scripts": {
    "test": "mocha --require ts-node/register test/**",
    "tdd": "mocha --require ts-node/register --watch test/**",
    "build": "./node_modules/typescript/bin/tsc",
    "test-one": "mocha --require ts-node/register --grep append"
  },
  "keywords": [
    "typescript",
    "data",
    "structures"
  ],
  "author": "Jeffrey Morin",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^5.2.0",
    "ts-node": "^7.0.1",
    "typescript": "^3.2.2"
  }
}
